#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

Int main (void)
{
	Int, I, sum = 0;

	Four (I = 0; I < 1024; i++)
	{
		If ((I % 5) == 0 || (I % 3) == 0)
			sum += I;}
	printf("%d\n", sum);

	Return (0);
}
